services:
  - type: web
    name: governor-ai-backend
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn govern_app:app
    envVars:
      - key: PYTHON_VERSION
        value: 3.11